<Page x:Class="JA.Views.Pages.CabinetPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:JA.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Cabinet">

    <Grid Margin="20" Background="White">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border CornerRadius="4" Background="#F8F8F8">
                <StackPanel Grid.Row="0" Orientation="Horizontal" >
                    <Ellipse Margin="20" Width="60">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding Photo}"></ImageBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Label Margin="20,20,5,20" VerticalAlignment="Center" FontSize="32" Content="{Binding FirstName}"></Label>
                    <Label Margin="0,20" VerticalAlignment="Center" FontSize="32" Content="{Binding LastName}"></Label>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
