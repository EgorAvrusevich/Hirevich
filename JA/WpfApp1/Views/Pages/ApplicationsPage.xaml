<Page x:Class="JA.Views.Pages.ApplicationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:JA.Views.Pages" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ApplicationsPage">

    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!-- Верхняя панель с поиском -->
        <Border Grid.Row="0" Height="40" Margin="20" Background="White" CornerRadius="4">
            <Grid>
                <Border CornerRadius="4" Padding="1" Margin="10,0,200,0" Background="#F8F8F8" Height="25">
                    <Grid>
                        <TextBox BorderThickness="0" Background="#F8F8F8" VerticalAlignment="Center" Margin="25,0,0,0"/>
                        <Image Source="/Images/lupa.png" Height="10" Width="10" HorizontalAlignment="Left" Margin="5"/>
                    </Grid>
                </Border>
                <Border Margin="0,0,10,0" Height="25" Width="180" HorizontalAlignment="Right" 
                        BorderThickness="0.3" BorderBrush="#849A9C" CornerRadius="4">
                    <Label Content="Фильтр" FontSize="10" HorizontalAlignment="Center" Foreground="#FF849A9C"/>
                </Border>
            </Grid>
        </Border>

        <!-- Список заявок -->
        <Grid Grid.Row="1" Margin="0">
            <ListBox x:Name="ApplicationsList" ItemsSource="{Binding Application}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="2" BorderBrush="Black" Margin="10">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Строка с названиями -->
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Grid.Column="0" Content="{Binding Vacation_Name}"/>
                                    <Label Grid.Column="1" Content="{Binding Company_name}" HorizontalAlignment="Right"/>
                                </Grid>

                                <!-- Строка с зарплатой -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0">
                                    <Image Width="15" Height="15" Source="/Images/Job wage.png"/>
                                    <Label Content="{Binding Wage}" Margin="5,0,0,0"/>
                                </StackPanel>

                                <!-- Описание с обрезкой -->
                                <Border Grid.Row="2" Margin="0,10,0,0">
                                    <TextBlock 
                                        Text="{Binding Description}"
                                        FontSize="14"
                                        TextWrapping="Wrap" 
                                        TextTrimming="CharacterEllipsis"
                                        MaxHeight="45"
                                        MaxWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"/>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Page>